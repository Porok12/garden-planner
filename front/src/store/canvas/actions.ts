import { actions } from "./reducers";

export const {
    enableOrbitControls, disableOrbitControls,
    enableSky, disableSky,
    setBrushSize, setBrushOpacity, setBrushColor, setBrush
} = actions;
